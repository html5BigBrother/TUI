{
  "name": "TUI-repo",
  "description": "TUI组件库工程",
  "author": "yty",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "pnpm run -C docs docs:dev",
    "docs:build": "pnpm run -C docs docs:build",
    "docs:serve": "pnpm run -C docs docs:serve",
    "test:components": "pnpm run -C packages/components test",
    "coverage:components": "pnpm run -C packages/components coverage",
    "release": "gulp release"
  },
  "dependencies": {
    "@t-ui/components": "workspace:*",
    "@t-ui/utils": "workspace:*",
    "t-ui-core": "workspace:*",
    "vite": "^3.1.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.3.0",
    "less": "^4.1.3",
    "typescript": "^4.8.4",
    "vue": "^3.2.36"
  },
  "engines": {
    "node": ">=16"
  }
}
